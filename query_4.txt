SELECT 
    AVG(
        1.0 * (
            (YEAR(s.AcArrDate) * 12 + MONTH(s.AcArrDate)) -
            (YEAR(p.OrderDate) * 12 + MONTH(p.OrderDate))
        )
    ) AS Avg_Lead_Time_Months
FROM PO p
JOIN Shipment s ON p.OID = s.OID
WHERE s.AcArrDate IS NOT NULL;